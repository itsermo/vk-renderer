{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "makefile (release)",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": ".."
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}